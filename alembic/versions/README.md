# Alembic versions

This directory will contain alembic revision scripts (one per migration).
Use `alembic revision --autogenerate -m "message"` to create a new revision and
`alembic upgrade head` to apply migrations.