[pytest]
pythonpath = .

filterwarnings =
    # Keep the passlib deprecation quiet
    ignore::DeprecationWarning:passlib.*
    # Suppress SQLAlchemy SAWarning noise during tests (transaction deassociated)
    # Match the SAWarning class explicitly so pytest filters it at collection time.
    ignore::sqlalchemy.exc.SAWarning
